# ReportMate Build Configuration
# Copy this file to .env and fill in your values
# DO NOT commit .env to the repository

# ═══════════════════════════════════════════════════════════════════════════
# API CONFIGURATION (for ReportMatePrefs package)
# ═══════════════════════════════════════════════════════════════════════════

# API URL - ReportMate backend endpoint
REPORTMATE_API_URL="https://your-api-url.example.com"

# Client Passphrase - for client-to-API authentication (X-Client-Passphrase header)
# This is what clients use to authenticate with the API
# Note: REPORTMATE_API_KEY is for internal cloud resources only (not used by clients)
REPORTMATE_PASSPHRASE="your-client-passphrase-here"

# ═══════════════════════════════════════════════════════════════════════════
# SIGNING CONFIGURATION
# ═══════════════════════════════════════════════════════════════════════════

# Team ID (found in Apple Developer account)
TEAM_ID="XXXXXXXXXX"

# Developer ID Application (for code signing the binary and app bundle)
# Example: "Apple Development: Your Name (XXXXXXXXXX)" for development
# Example: "Developer ID Application: Your Organization (XXXXXXXXXX)" for distribution
SIGNING_IDENTITY_APP="Apple Development: Your Name (XXXXXXXXXX)"

# Developer ID Installer (for signing the PKG installer)
# Example: "Developer ID Installer: Your Organization (XXXXXXXXXX)"
SIGNING_IDENTITY_INSTALLER="Developer ID Installer: Your Organization (XXXXXXXXXX)"

# Optional: Specify keychain path if not using default login keychain
# SIGNING_KEYCHAIN="${HOME}/Library/Keychains/login.keychain-db"

# Enable timestamp during signing (recommended)
SIGNING_TIMESTAMP="true"

# ═══════════════════════════════════════════════════════════════════════════
# NOTARIZATION CONFIGURATION
# ═══════════════════════════════════════════════════════════════════════════

# Keychain profile name for notarization credentials
# Create with: xcrun notarytool store-credentials "PROFILE_NAME" --apple-id "you@example.com" --team-id "XXXXXXXXXX"
NOTARIZATION_KEYCHAIN_PROFILE="notarization_credentials"

# Optional: Apple ID for notarization (if not using keychain profile)
# NOTARIZATION_APPLE_ID="you@example.com"
# NOTARIZATION_TEAM_ID="XXXXXXXXXX"
# NOTARIZATION_PASSWORD="@keychain:AC_PASSWORD"

# ═══════════════════════════════════════════════════════════════════════════
# BUILD CONFIGURATION
# ═══════════════════════════════════════════════════════════════════════════

# Version override (optional - defaults to YYYY.MM.DD.HHMM)
# VERSION="2025.01.01.1200"
